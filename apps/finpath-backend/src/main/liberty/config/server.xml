<?xml version="1.0" encoding="UTF-8"?>
<server description="finpath">

    <!-- ==== Features ==== -->
    <featureManager>
        <feature>webProfile-10.0</feature>
        <feature>jdbc-4.3</feature>
        <feature>restfulWS-3.1</feature>
        <feature>jsonb-3.0</feature>
        <feature>mpHealth-4.0</feature>
    </featureManager>

    <!-- ==== HTTP Endpoint ==== -->
    <httpEndpoint id="defaultHttpEndpoint" httpPort="9080" httpsPort="9443" />

    <!-- ==== Basic Registry (fixes ORB/user registry warning) ==== -->
    <basicRegistry id="basic" realm="BasicRealm">
        <user name="admin" password="admin123" />
    </basicRegistry>

    <!-- ==== Application Manager ==== -->
    <applicationManager autoExpand="true" />

    <!-- ==== PostgreSQL JDBC Library ==== -->
    <library id="postgresLib">
        <!-- Pfad zu deinem PostgreSQL-JDBC-Jar im Liberty-Config-Verzeichnis -->
        <fileset dir="${server.config.dir}/resources" includes="postgresql*.jar" />
    </library>

    <!-- ==== DataSource (FIXE Werte) ==== -->
    <dataSource id="FinPathDS" jndiName="jdbc/FinPathDS" type="javax.sql.DataSource">
        <jdbcDriver
                libraryRef="postgresLib"
                javax.sql.DataSource="org.postgresql.ds.PGSimpleDataSource" />
        <properties
                serverName="localhost"
                portNumber="5432"
                databaseName="finpath"
                user="finpath"
                password="secret" />
    </dataSource>

    <!-- ==== Application Deployment ==== -->
    <application
            id="finpath"
            name="finpath"
            location="finpath.war"
            type="war"
            contextRoot="/" />
</server>