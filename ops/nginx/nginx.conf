events {
    worker_connections 512;
}

http {
    access_log /dev/stdout;
    error_log /dev/stderr warn;

    # PRODUCTION - css-appli24.com
    server {
        listen 80;
        server_name css-appli24.com www.css-appli24.com;

        # API Routing
        location /api/ {
            proxy_pass http://backend-prod:9080;
            proxy_set_header Host $host;
        }

        # Frontend Routing
        location / {
            proxy_pass http://frontend-web-prod:80;
            proxy_set_header Host $host;
        }
    }

    # DEVELOPMENT - dev.css-appli24.com
    server {
        listen 80;
        server_name dev.css-appli24.com;

        # API Routing
        location /api/ {
            proxy_pass http://backend-dev:9080;
            proxy_set_header Host $host;
        }

        # Frontend Routing
        location / {
            proxy_pass http://frontend-web-dev:80;
            proxy_set_header Host $host;
        }
    }

    # Default - block unknown requests
    server {
        listen 80 default_server;
        return 444;
    }
}