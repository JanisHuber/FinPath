services:
  proxy:
    image: nginx:alpine
    container_name: proxy
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    expose:
      - "8080"
      - "8081"
    networks:
      - finpath-net
    restart: always

networks:
  finpath-net:
    external: true
