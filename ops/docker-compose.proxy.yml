services:
  proxy:
    image: nginx:alpine
    container_name: proxy
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - "80:80"
    networks:
      - finpath-net
    restart: always

networks:
  finpath-net:
    external: true
